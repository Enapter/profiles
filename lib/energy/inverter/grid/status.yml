blueprint_spec: profile/1.0

display_name: Grid Connection Status
description: Grid connection state for grid-tied and hybrid inverters.

telemetry:
  grid_status:
    display_name: Grid Connection Status
    type: string
    enum:
      disconnected:
        display_name: Disconnected
        description: Inverter is not connected to the grid. This includes off-grid or island mode, pre-synchronization, or operator-initiated disconnection.
      connected:
        display_name: Connected
        description: Operating in grid-connected mode, grid connection is active.
      fault:
        display_name: Fault
        description: Grid parameters out of specified range (voltage, frequency, etc.), grid connection is temporarily disabled.
