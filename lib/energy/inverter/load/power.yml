blueprint_spec: profile/1.0

display_name: Inverter Load Power
description: Power consumed by local loads on inverter AC output or fed back to grid/battery.

telemetry:
  load_total_power:
    display_name: Load Power
    type: float
    unit: W
    description: Total power consumed by local loads on inverter AC output (positive) or fed back to grid/battery (negative).
